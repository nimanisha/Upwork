apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.applicationName }}-svc"
spec:
  selector:
    app: {{ .Values.applicationName }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      